document.querySelectorAll('.date').forEach(card=>{card.addEventListener('pointermove',e=>{if(matchMedia('(prefers-reduced-motion: reduce)').matches)return;const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`translateY(-8px) rotateX(${-y*2}deg) rotateY(${x*2}deg)`});card.addEventListener('pointerleave',()=>card.style.transform='')});\n\nconst fateBell = document.getElementById('fate-bell');\nconst assistanceCopy = document.getElementById('assistance-copy');\nconst assistanceSection = document.querySelector('.house-assistance');\n\nif (fateBell && assistanceCopy) {\n  const dates = ['bad-decision/', 'yearner/', 'adventure/', 'sweetheart/', 'aristocrat/'];\n  let choosing = false;\n\n  fateBell.addEventListener('click', () => {\n    if (choosing) return;\n    choosing = true;\n    const destination = dates[Math.floor(Math.random() * dates.length)];\n    const reducedMotion = matchMedia('(prefers-reduced-motion: reduce)').matches;\n\n    fateBell.classList.add('is-ringing');\n    assistanceCopy.textContent = 'The House is considering your predicament…';\n\n    window.setTimeout(() => {\n      assistanceSection?.classList.add('is-decided');\n      assistanceCopy.textContent = 'A decision has been made.';\n    }, reducedMotion ? 250 : 1050);\n\n    window.setTimeout(() => {\n      window.location.href = destination;\n    }, reducedMotion ? 750 : 1850);\n  });\n}\n